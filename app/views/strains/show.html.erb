<h1><%= @strain.name%>  </h1>


<h2> <%= @strain.category %> </h2>
<div>
  <p><%= @strain.thc %></p>
  <p><%= @strain.cbd %></p>
</div>


<%= link_to "Edit #{@strain.name}", edit_strain_path(@strain) if @strain.users == current_user%>

<br></br>

<%= link_to "See all Reviews", strain_reviews_path(@strain) %>

